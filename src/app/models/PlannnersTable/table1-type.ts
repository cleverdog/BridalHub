export interface Table1Type {
  planner_id: string;
  customer_id: string;
  planner_name: string;
  gender: string;
  birth_date: string;
  registration_date: string;
  position: string;
  photo: string;
}
